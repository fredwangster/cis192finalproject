import urllib2
import time

'''Scans scraped source and retrieves domestic urls'''
def crawler(url_list, crawl_depth):
    start = time.time()
    dir_name = './output_files'
    for url in url_list:
        url_name = url.replace("/","").replace(":","")
        html_name = 
        for line in open(html_name):
            
